import 'package:flutter/material.dart';

class InternetHelpView extends StatelessWidget {
  const InternetHelpView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
        return Center(child: Text("help view"),);

  }
}